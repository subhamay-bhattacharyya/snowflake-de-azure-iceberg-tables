# -- infra/snowflake/tf/modules/external_volume/versions.tf (Child Module)

terraform {
  required_providers {
    snowflake = {
      source  = "snowflakedb/snowflake"
      version = "0.99.0"
    }
  }
}
